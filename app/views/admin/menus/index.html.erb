<i>Menu Articles</i>
  <p style="display:inline;float:right;"<%= link_to("Dodaj Artykuł", new_admin_article_path)%>
  </p>
<table>
  <tr id="none">
    <th>#id</th>
    <th>Menu?</th>
    <th>Menu position</th>
    <th>publish</th>
    <th>title</th>
    <th colspan="4">actions</th>
  </tr>
  <% @articles.each do |article| %>
  <tr>
    <td>#<%= article.id %></td>
    <%# article.menu_position %>
    <td><%= if !Menu.find(:first, :conditions => {:article_id => article.id }).nil? then link_to("Tak", article_menu_path(article, "no")) else link_to("Nie", article_menu_path(article, "yes")) end  %></td>
    <%# if article.menu then link_to("Tak", article_menu_path(article, "no")) else link_to("Nie", article_menu_path(article, "yes")) end  %>
    <td><%= article.position %></td>
    <td><%= article.title %></td>
    <td><%= link_to("Up", article_menu_path(article, "up"))%> |
        <%= link_to("Down", article_menu_path(article, "down"))%>
    </td>
    <td><%= link_to("Pokaż", admin_article_path(article)) %></td>
    <td><%= link_to("Edytuj", edit_admin_menu_path(article))%></td>
    <td><%= link_to "Usuń", [:admin, article], :confirm => 'TAK?', :method => :delete %></td>
  </tr>
  <% end %>
</table>